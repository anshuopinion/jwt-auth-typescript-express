import { Schema, model, Document } from "mongoose";

export interface I{{pascalCase name}} extends Document {
 
}

const {{pascalCase name}}Schema: Schema = new Schema({
 
});

export default model<I{{pascalCase name}}>("{{camelCase name}}", {{pascalCase name}}Schema);