import { InternalServerError } from "http-errors";
import { RequestHandler } from "express";
import {{pascalCase name}} from "../model/{{pascalCase name}}";

export const get{{pascalCase name}}ById: RequestHandler = async (req, res, next) => {
  const { id } = req.params;

  try {
    const {{camelCase name}} = await {{pascalCase name}}.findById(id);
    res.json({{camelCase name}});
  } catch (error) {
    return next(InternalServerError);
  }
};
